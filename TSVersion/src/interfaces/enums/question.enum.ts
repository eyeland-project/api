export enum QuestionType {
  FLASHCARD = "flashcard",
  FILL = "fill",
  ORDER = "order",
  SELECT = "select",
  AUDIO = "audio"
}

export enum QuestionTopic {
  VOCABULARY = "vocabulary",
  PREPOSITIONS = "prepositions"
}
